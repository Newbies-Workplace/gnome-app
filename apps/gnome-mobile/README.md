# FAQ

- Backend calls are not working in the mobile app (`Fetch error: [AxiosError: Network Error]`)
  - Make sure you run proxy using `yarn proxy` after starting the emulator

# Translations

## Checking for hardcoded strings

``` bash
yarn i18n:lint
```

## Extracting new strings

``` bash
yarn i18n:extract
```
